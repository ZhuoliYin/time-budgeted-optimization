# time-budgeted-optimization
In this branch, we will focus on learn to order function, regardless of assigning service time.

New features will be added to the input of neural network

Actually service time is unknow a priori until an action is taken. The env will generate the actual service time.
